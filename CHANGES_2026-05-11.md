# Changes — 2026-05-11

Summary of all changes in this session. Branch: `main`. 4 commits ahead of `origin/main` (not yet pushed).

---

## 1. Page redesigns

### `d2c05b5` — software / web / mobile pages → site design system (37 files)
- All 34 sections rewritten to use the shared shell + components (`SectionHeader`, `ServiceCard`, `FAQItem`, `CTAButton`)
- Calm image-bg hero replaces the 2-column hype heroes (no more fake Unsplash avatars, "200+" pills, animated badges)
- Image-bg final CTA matching the AI page; spacers normalized to `h-6 sm:h-8`
- All copy, FAQ schema, and JSON-LD preserved verbatim

### `56c806a` — tech-companies-nepal → site design system (8 files)
- Standardized non-standard padding and bg pattern across all sections
- Replaced hand-rolled FAQ accordion with shared `<FAQItem>` (FAQPage JSON-LD intact)
- Replaced blur-orb / Trophy-icon CTA with the standard image-bg CTA
- Removed brand-colored category pills and the scaled "Most Popular" pricing card

### `0e240a1` — careers page (1 file)
- Added **Full Stack Engineer Intern** opening (positioned first in the list)
- Added matching `fullstack-engineer-intern` option to the application form's position dropdown

---

## 2. SEO work — bulk of the session

### New SEO landing page: `/it-company-in-kathmandu-nepal`
**Commit:** `bfa968e` — *add IT company in Kathmandu, Nepal SEO landing page*

Built from the SEO engineer's brief, treated as guidance rather than copied verbatim.

**Target keywords (per the brief):**
- IT company in Kathmandu, Nepal *(primary)*
- best IT company Kathmandu Nepal
- IT companies in Kathmandu Nepal
- IT firms in Kathmandu Nepal
- IT software companies in Kathmandu Nepal
- IT solution company in Kathmandu Nepal
- software company in Kathmandu Nepal
- AI company in Nepal
- website development company Kathmandu

**On-page SEO:**
- `<title>`: `IT Company in Kathmandu, Nepal | Noble Stack` (exact phrasing from brief)
- Meta description, OG title/description, canonical URL, and `keywords` array — all set as specified
- Single H1 on the page: *"IT Company in Kathmandu, Nepal — AI, Software & Web Development"* (primary keyword first)
- H2s on each section repeat target keywords naturally (e.g., *"Our IT Services in Kathmandu, Nepal"*, *"Why Noble Stack Is a Trusted IT Company in Kathmandu"*)
- Slug `/it-company-in-kathmandu-nepal` matches the primary keyword
- Static rendering (`force-static`) so the page is fully crawlable

**Structured data (JSON-LD):**
- `LocalBusiness` — Saraswati Marg, Kathmandu address + geo coords (27.7172, 85.3240) + opening hours + areas served (Nepal, Singapore, US, Australia, UK)
- `Service` — IT Services in Kathmandu, Nepal, linked to the LocalBusiness
- `FAQPage` — 7 Q&As (mirrors the on-page FAQs so Google can show rich result FAQs in SERPs)
- `BreadcrumbList` — Home → IT Company in Kathmandu, Nepal

**Sitemap:** added `/it-company-in-kathmandu-nepal` to `src/app/sitemap.ts` with priority 0.8.

**Editorial choices vs. the brief** (you said "thin guideline, don't overdo it"):
- Cut three near-duplicate sections in the brief that all repeated "best company in Kathmandu" — kept one strong intro and one strong "Why Noble Stack" instead
- Condensed 6 service deep-dives (each with 8+ sub-bullet lists) into a clean 6-card grid — preserves the keyword coverage without doubling page length
- Folded standalone "UI/UX Design" section into the services grid as one of six cards
- Tightened FAQ answers from sales-y prose into direct, useful answers

**Page structure (9 sections):**
Hero → Intro → Services (6 cards) → Why Noble Stack → Industries (10 items) → Process (6 steps) → Tech Stack (6 categories) → FAQ (7 items) → Final CTA

### Home page SEO sprinkle (same commit `bfa968e`)
Light-touch updates so the home page can also rank for the IT/AI keyword set — no new sections, no layout changes.

**`home/layout.tsx` metadata:**
- New `<title>`: `Noble Stack — AI-First IT Company in Kathmandu, Nepal | Web, Software, Mobile & AI`
- Description, OpenGraph, and Twitter cards rewritten to lead with the IT company phrasing
- `keywords` array reordered/expanded — leads with `IT company in Kathmandu Nepal`, `best IT company Kathmandu Nepal`, `software company in Kathmandu Nepal`, `AI company in Nepal`, then the existing AI/web keywords (kept Noble Stack identity intact)

**Visible copy sprinkle (just two spots, kept natural):**
- **Hero subtitle (`L1.tsx`):** *"Noble Stack is recognized as one of the best AI and IT companies in Kathmandu, Nepal."* — H1 left untouched (still a brand statement)
- **About section (`AboutUs.tsx`):** heading now reads *"About Noble Stack — Best AI & IT Company in Kathmandu, Nepal"*; description includes *"recognized as one of the best AI companies in Nepal"*

The home page already had a `LocalBusiness` schema in `home/layout.tsx` — left as-is since it's already well-formed.

---

## Outstanding SEO items worth a follow-up

- **Sitemap gap:** `/software-development-company-nepal`, `/web-development-company-nepal`, `/mobile-app-development-company-nepal`, and `/tech-companies-nepal` are **still missing from `src/app/sitemap.ts`**. Only `/ai-development-company-in-nepal`, `/hire-ai-developers`, and the new `/it-company-in-kathmandu-nepal` are listed. This means Google is unlikely to index those four pages from the sitemap. Worth a separate commit.
- **No internal links yet** from the home page, services pages, or footer to the new `/it-company-in-kathmandu-nepal` page — only the sitemap entry. Adding it to a footer or services index would help PageRank flow.
- 4 commits **not yet pushed** to `origin/main`.

---

## Verification
- `npx tsc --noEmit` passes
- `npm run build` succeeds — 26 static pages including the new one
